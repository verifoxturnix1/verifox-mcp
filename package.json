{
  "name": "verifox-mcp",
  "version": "1.1.1",
  "description": "Verifox MCP server — verify email deliverability and find business emails (single + bulk) for Claude and any MCP client.",
  "type": "module",
  "license": "MIT",
  "homepage": "https://verifox.ai",
  "bugs": "https://verifox.ai",
  "keywords": [
    "mcp",
    "modelcontextprotocol",
    "model-context-protocol",
    "claude",
    "email-verification",
    "email-verifier",
    "email-finder",
    "email-deliverability",
    "verifox"
  ],
  "main": "dist/index.js",
  "bin": {
    "verifox-mcp": "dist/index.js"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json && chmod +x dist/index.js",
    "start": "bun run src/index.ts",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "@types/node": "^20.19.0"
  }
}
